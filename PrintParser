public void parsePrintStatement() {
  Token from = null; //not sure what goes here
  string variable = null;
 
  myTokenizer.getToken(); // Deletes 'PRINT' from strong
  if (myTokenizer.peekToken().getType() == Token.T_PRINT)
  {
   myTokenizer.getToken();
  } 
  else if (myTokenizer.peekToken().getType() == Token.T_VARIABLE)//is this supposed to be variable? Planned format was "PRINT min/max/median (have we even done those yet??) FROM [    ]
  {
   myTokenizer.getToken();
  }
  else if (myTokenizer.peekToken().getType() == Token.T_FROM)
  {
    myTokenizer.getToken();
  }
  else if (myTokenizer.peekToken().getType() == Token.T_VARIABLE)//or is this supposed to be variable?
  {
    myTokenizer.getToken();
  }
}
